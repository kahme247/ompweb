// Test stub for components/ui/toast.tsx: jiti cannot parse the real TSX file
// and the DOM-backed toast must never fire inside Node tests.
export const toast = {
  success() {},
  error() {},
  info() {},
  close() {},
};
export default toast;
